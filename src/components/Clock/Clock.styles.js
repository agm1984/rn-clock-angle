import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  clockContainer: {
    alignItems: 'center',
    backgroundColor: '#fff',
    paddingTop: 16,
    paddingBottom: 24,
  },
})
